export const defineType = <T>(def: T): T => def
export const defineField = <T>(def: T): T => def
export const defineArrayMember = <T>(def: T): T => def

export default {
  defineType,
  defineField,
  defineArrayMember,
}
